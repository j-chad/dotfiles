#!/bin/sh
# Scans staged changes for secrets when Betterleaks is installed.
set -eu

command -v betterleaks >/dev/null 2>&1 || exit 0
betterleaks git --pre-commit --redact=95 --staged --no-banner --log-level=warn --verbose
